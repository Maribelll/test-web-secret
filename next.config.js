/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
};

module.exports = nextConfig;
module.exports = {
  images: {
    domains: ["example.com", "example2.com"],
  },
};
